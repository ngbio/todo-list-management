function Footer() {
    return (
        <footer className="app-footer">
            <div className="container footer-inner">
                <span>Todo List Management</span>
                <span>Flask backend, ReactJS frontend</span>
            </div>
        </footer>
    );
}

export default Footer;
